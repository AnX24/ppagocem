export function ingreso(primera, segunda){

    var auxA = primera;
    var auxB = segunda;

    if (!isNaN(primera.charat(0))){
        alert("es un numero");
    }
    else alert("no es un numero");

    return null;
}